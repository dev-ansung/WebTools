tailwind.config = {
  theme: {
    extend: {
      maxWidth: {
        page: '30rem',       // tool card width — comfortable for forms
        'page-index': '48rem', // index grid width — allows 3 columns
      }
    }
  }
}
